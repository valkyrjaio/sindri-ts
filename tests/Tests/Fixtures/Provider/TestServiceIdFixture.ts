// Fixture parsed by ts-morph (never executed).
/* eslint-disable */
// @ts-nocheck
// Mirrors how the framework declares its binding keys: `as const`, not a bare string literal.
export class TestServiceIdFixture {
    static readonly Data = 'svc.const.data' as const;
    static readonly Contract = 'svc.const.contract' as const;
}
