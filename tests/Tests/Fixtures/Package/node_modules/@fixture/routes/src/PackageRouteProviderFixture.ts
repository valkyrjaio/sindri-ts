// Fixture parsed by ts-morph (never executed).
/* eslint-disable */
// @ts-nocheck
import { PackageCommandName } from './PackageCommandNameFixture.ts';

export class PackageRouteProviderFixture {
    getRoutes() {
        return [new Route(PackageCommandName.LIST, 'List things', PackageRouteProviderFixture.listHandler)];
    }
}
