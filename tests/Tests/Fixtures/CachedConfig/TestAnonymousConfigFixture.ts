// Fixture parsed by ts-morph (never executed). The class has no name, so there
// is nothing to generate a cached config from.
/* eslint-disable */
// @ts-nocheck
import { HttpConfig } from './HttpConfig.ts';

export default class extends HttpConfig {
    constructor() {
        super('App');
    }
}
