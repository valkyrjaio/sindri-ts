// Fixture parsed by ts-morph (never executed). Neither extends a known config
// base nor implements a known config contract, so it cannot be read.
/* eslint-disable */
// @ts-nocheck

export class TestNoContractConfigFixture implements SomethingElseContract {
    readonly namespace: string = 'App';
}
