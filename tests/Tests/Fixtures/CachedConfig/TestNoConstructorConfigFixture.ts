// Fixture parsed by ts-morph (never executed). Extends a known base but declares
// no constructor, so there are no super() arguments to read.
/* eslint-disable */
// @ts-nocheck
import { HttpConfig } from './HttpConfig.ts';

export class TestNoConstructorConfigFixture extends HttpConfig {}
