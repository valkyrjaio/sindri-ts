// Fixture parsed by ts-morph (never executed). Implements a known contract but
// declares no fields at all.
/* eslint-disable */
// @ts-nocheck

export class TestEmptyConfigFixture implements ConfigContract {}
