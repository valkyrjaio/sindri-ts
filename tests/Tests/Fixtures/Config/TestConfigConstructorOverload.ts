// Fixture parsed by ts-morph (never executed) — an ambient class whose
// constructor has no body, exercising the no-body guard.
/* eslint-disable */
// @ts-nocheck
declare class TestConfigConstructorOverload extends Config {
    constructor(namespace: string);
}
