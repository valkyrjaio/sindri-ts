// Fixture parsed by ts-morph (never executed).
/* eslint-disable */
// @ts-nocheck
export class Config extends BaseConfig {
    constructor() {
        super(
            'App',
            __dirname,
            '1.0.0',
            'production',
            false,
            'UTC',
            'secret',
            'Data',
            'App.Data',
            'App.Provider.Data',
            [AppComponentProvider],
        );
    }
}
