// Fixture parsed by ts-morph (never executed).
/* eslint-disable */
// @ts-nocheck
export class AppServiceProviderFixture {
    publishers() {
        return {
            'app.service': AppService.publish,
        };
    }
}
